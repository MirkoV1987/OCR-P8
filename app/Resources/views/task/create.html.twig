{% extends 'base.html.twig' %}

{% block body %}
        <div class="row">
            {{ form_start(form, {'action' : path('task_todo')}) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-success pull-right text-uppercase">Ajouter</button>
            {{ form_end(form) }}

            <a href="{{ path('task_list') }}" class="btn btn-primary text-uppercase">Retour à la liste des tâches</a>
        </div>
{% endblock %}
